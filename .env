# The site's public address. Used to build absolute URLs for the link-preview
# meta tags in index.html, which crawlers require. Change this to the real
# domain before deploying — no trailing slash.
VITE_SITE_URL=https://shribuildcon.in
