# The site's public address, used to build the absolute URLs that link-preview
# crawlers require. This value is only the local fallback: on Netlify the build
# command passes the site's real address ($URL) instead, so previews stay correct
# on the netlify.app subdomain and on the custom domain once one is attached.
VITE_SITE_URL=https://shreebuildcon.in
